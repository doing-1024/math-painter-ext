{
  "name": "math-painter-ext",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Official plugin repository for math-painter (hosted on Cloudflare Pages).",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "katex": "^0.16.0",
    "typescript": "^5.5.0"
  }
}
